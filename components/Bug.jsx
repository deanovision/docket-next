import React from "react";
import { Card, CardBody, CardHeader } from "@chakra-ui/react";

function Bug({ created, createdBy, description, status }) {
  return <Card>Bug Card</Card>;
}
export default Bug;
